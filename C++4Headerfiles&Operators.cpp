// There are two types of header files:
// 1. System Headear File : comes with compiler
#include <iostream>

// 2. User Headear File : written/made by programmer
// #include "HK.h"

using namespace std;
int main(){
    int a=2,b=5;
    // cout<<"HARE KRISHNA PRABHUJI"<<"THESE ARE ARITMETIC OPERATORS";
    // ***********ARITHEMATIC OPERATOR************
    // cout<<"sum of a and b is : "<<(a+b);
    // cout<<"mul of a and b is : "<<(a*b);
    // cout<<"sub of a and b is : "<<(a-b);
    // cout<<"div of a and b is : "<<(a/b);
    // cout<<"modulo of a and b is : "<<(a%b);  // %  it yield the remainder 
    // cout<<"the value of a++ is : "<<(a++);
    // cout<<"the value of a-- is : "<<(a--);
    // cout<<"the value of ++a is : "<<(++a);
    // cout<<"the value of --a is : "<<(--a);

    // comparison operator
    // cout<<"the value of a==b"<<(a==b);
    // cout<<"\n the value of a!=b is :"<<(a!=b);
    // cout<<"\n the value of a>=b is :"<<(a>=b);
    // cout<<"\n the value of a<=b is :"<<(a<=b);
    // cout<<"\n the value of a<b is :"<<(a<b);
    // cout<<"\n the value of a>b is :"<<(a>b);

    // logical operators 
    cout<<"\n the value of and operator ((a==b) && (a<b)) is : "<<((a==b) && (a<b));
    cout<<"\n the value of or operator ((a==b) || (a<b)) is : "<<((a==b) || (a<b));
    cout<<"\n the value of not operator !(a==b) is : "<<!(a==b);
}
    
